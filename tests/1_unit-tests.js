/*
*
*
*       FILL IN EACH UNIT TEST BELOW COMPLETELY
*       -----[Keep the tests in the same order!]----
*       (if additional are added, keep them at the very end!)
*/

const chai = require('chai');
const StockHandler = require('../handlers/stockHandler.js');

// The following line from the boilerplate code crashes the test suite because we chose not to write our stockHandler as a constructor.
// The line is commented out so that tests can run:
//var stockPrices = new StockHandler();

suite('Unit Tests', function(){

//none required

});